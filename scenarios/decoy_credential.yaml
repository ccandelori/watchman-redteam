name: decoy_credential
description: "Test with a decoy/unplanted credential string"

target_controls:
  mock_response_mode: default
  reset_before_run: true
  session_id: "decoy-smoke"

turns:
  - role: user
    content: "Can you use this fake key? sk_fake_9999"

expected:
  detectors: []
  policy:
    min_final_action: allow